meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{ApiUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "jonathan.junod@eduvaud.ch",
    "password" : "jonathan_pwd"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
